NEXT_PUBLIC_API_BASE_URL=http://localhost:3001
API_BASE_URL=http://localhost:3001
TOKEN_AUTH=changeme
GAME_SERVER_HTTP_URL=http://localhost:7666

# Tiene que valer EXACTAMENTE lo mismo que GAME_DATA_ADMIN_PROXY_TOKEN en la
# api. Si la api lo tiene seteado y aca falta, el editor de plantillas de NPCs
# e items responde 403 (la api lo avisa en su log).
GAME_DATA_ADMIN_PROXY_TOKEN=
